apiVersion: v1
kind: Service
metadata:
  name: {{ include "travel-ground.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "travel-ground.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "travel-ground.selectorLabels" . | nindent 4 }}
