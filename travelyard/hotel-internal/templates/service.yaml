apiVersion: v1
kind: Service
metadata:
  name: {{ include "hotel-internal.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "hotel-internal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hotel-internal.selectorLabels" . | nindent 4 }}
