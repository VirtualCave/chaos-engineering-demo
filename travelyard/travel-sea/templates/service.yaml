apiVersion: v1
kind: Service
metadata:
  name: {{ include "travel-sea.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "travel-sea.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "travel-sea.selectorLabels" . | nindent 4 }}
