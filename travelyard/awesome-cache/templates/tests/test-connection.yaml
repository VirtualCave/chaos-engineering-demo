apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "awesome-cache.fullname" . }}-test-connection"
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "awesome-cache.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "awesome-cache.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
