apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "travel-avail.fullname" . }}-test-connection"
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "travel-avail.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "travel-avail.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
