apiVersion: v1
kind: Service
metadata:
  name: {{ include "travel-avail.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "travel-avail.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "travel-avail.selectorLabels" . | nindent 4 }}
