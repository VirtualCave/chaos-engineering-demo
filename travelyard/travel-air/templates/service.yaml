apiVersion: v1
kind: Service
metadata:
  name: {{ include "travel-air.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "travel-air.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "travel-air.selectorLabels" . | nindent 4 }}
