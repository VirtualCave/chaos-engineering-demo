apiVersion: v1
kind: Service
metadata:
  name: {{ include "ground-bus.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "ground-bus.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ground-bus.selectorLabels" . | nindent 4 }}
