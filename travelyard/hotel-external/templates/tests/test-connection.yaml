apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hotel-external.fullname" . }}-test-connection"
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "hotel-external.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hotel-external.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
