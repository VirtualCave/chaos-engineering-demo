apiVersion: v1
kind: Service
metadata:
  name: {{ include "hotel-external.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "hotel-external.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hotel-external.selectorLabels" . | nindent 4 }}
