apiVersion: v1
kind: Service
metadata:
  name: {{ include "acid-slime.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "acid-slime.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "acid-slime.selectorLabels" . | nindent 4 }}
